# Copy to .env for local development — never commit .env

HTTP_PORT=3000
HTTPS_PORT=3443
NODE_ENV=development

# MySQL — set these to connect to a local or remote database
DB_HOST=localhost
DB_PORT=3306
DB_USER=hellonode
DB_PASSWORD=changeme
DB_NAME=hellonode_dev
